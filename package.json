{
  "name": "malrot-app",
  "scripts": {
    "test": "jest",
    "format": "prettier --write .",
    "lint": "eslint . --fix",
    "build": "npm run format"
  },
  "devDependencies": {
    "eslint": "^8.14.0",
    "jest": "^28.0.3",
    "prettier": "^2.6.2"
  },
  "eslintConfig": {
    "extends": [
      "eslint:recommended"
    ],
    "env": {
      "node": true,
      "jest": true,
      "es2021": true
    }
  }
}
